apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "passnet.fullname" . }}-test-connection"
  labels:
    {{- include "passnet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "passnet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
