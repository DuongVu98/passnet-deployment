apiVersion: v1
kind: Service
metadata:
  name: {{ include "passnet.fullname" . }}
  labels:
    {{- include "passnet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "passnet.selectorLabels" . | nindent 4 }}
